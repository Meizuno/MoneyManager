version: "3.9"

services:
  app:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: file:/app/data/db.sqlite
      NUXT_HOST: 0.0.0.0
      NUXT_PORT: "3000"
    ports:
      - "3000:3000"
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
